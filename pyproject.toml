[project]
name = "pet"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "pytest>=9.0.3",
    "ruff>=0.15.12",
]

[tool.uv.workspace]
members = [
    "api_gateway",
    "auth_service",
    "inventory_service",
    "kafka_broker",
    "notification_service",
    "order_service",
    "product_service",
    "user_service",
]
